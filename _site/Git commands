Updating files into GitHub:

git checkout -b <branchname> //create a new local branch to work in. 

git add // Move provisional changes to index to be committed

git commit // Save changes to local repo, create snapshot of repo

git push origin <branchname> //push changes to a new branch in the remote repo

git checkout rory1 //switch to local copy of master branch

git pull // pull all recent changes to central copy of repo

git merge rory1 // merge changes with